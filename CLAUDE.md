# CLAUDE.md

Personal landing page: one static `index.html`, photos in `assets/img/`, published by GitHub Pages from `main`.

- Before any change, read `docs/core/LESSONS.md` and `docs/core/SUGGESTIONS.md`.
- Every audit: add new beauty/polish ideas to `SUGGESTIONS.md`, one line each; tick shipped ones.
- Every mistake that cost a fix: add one line to `LESSONS.md` (rule + why).
- Keep docs brief. No filler. Page copy is Spanish (Argentine voseo); docs for Claude are English.
- If editing changes structure, photos or contact data, update `docs/core/EDITING.md`.
- Don't leave dev servers running; verify, then stop them.
